{% extends "base.twig" %}

{% block title %}Change Password | PixSalle{% endblock %}

{% block content %}
    <link href="/css/changePassword.css" rel="stylesheet">

    <h1>Reset Password</h1>
    <div id="main-container">
        <form action="{{ formAction }}" method="POST">
            <div class="input-group mb-3">
                <span class="input-group-text" id="basic-addon1">Old Password</span>
                <input type="password" name="oldPassword" class="form-control">
            </div>
            {% if formErrors['wrongOldPassword'] %}
                <p style="color: red;" data-cy="changePass__wrongOldPassword">{{ formErrors['wrongOldPassword'] }}</p>
            {% endif %}
            <div class="input-group mb-3">
                <span class="input-group-text" id="basic-addon1">New Password</span>
                <input type="password" name="newPassword" class="form-control">
            </div>
            {% if formErrors['formatNewPassword'] %}
                <p style="color: red;"
                   data-cy="changePass__wrongFormatNewPassword">{{ formErrors['formatNewPassword'] }}</p>
            {% endif %}
            <div class="input-group mb-3">
                <span class="input-group-text" id="basic-addon1">Repeat password</span>
                <input type="password" name="confirmPassword" class="form-control">
            </div>
            {% if formErrors['newPassMatch'] %}
                <p style="color: red;" data-cy="changePass__newPassMatch">{{ formErrors['newPassMatch'] }}</p>
            {% endif %}
            {% if formErrors['formatMatchPassword'] %}
                <p style="color: red;"
                   data-cy="changePass__wrongFormatMatchPassword">{{ formErrors['formatMatchPassword'] }}</p>
            {% endif %}
            {% if  formErrors['passDoNotMatch'] %}
                <p style="color: red;" data-cy="changePass__passDoNotMatch">{{ formErrors['passDoNotMatch'] }}</p>
            {% endif %}
            <div id="buttons">
                <input type="submit" value="Reset Password" class="btn btn-primary">
            </div>
        </form>
    </div>

{% endblock %}